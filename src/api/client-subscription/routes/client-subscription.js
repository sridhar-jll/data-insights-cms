'use strict';

/**
 * client-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::client-subscription.client-subscription');
